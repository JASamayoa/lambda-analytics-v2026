{
  "name": "lambda-learn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "lambda Learn — landing y captura de leads para learn.lambda-analytics.net",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "db:migrate": "node scripts/run-migration.js",
    "test:api:local": "node scripts/test-api.js"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "resend": "^4.0.1"
  }
}
